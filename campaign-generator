#!/usr/bin/env python3
"""
Campaign Generator CLI Executable

Usage:
    ./campaign-generator generate -f campaign.yaml
    ./campaign-generator generate -f campaign.yaml -o outputs/ -v
    ./campaign-generator generate -f campaign.yaml --skip-compliance
"""
import sys
from pathlib import Path

# Add src to path
sys.path.insert(0, str(Path(__file__).parent))

from src.cli.main import cli

if __name__ == '__main__':
    cli()
